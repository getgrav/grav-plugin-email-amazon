{
    "name": "getgrav/email-amazon-tests",
    "description": "Test harness for the Email Amazon plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin stays free of development packages.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailAmazon\\": "../classes/",
            "Grav\\Plugin\\EmailAmazon\\Tests\\": "./"
        }
    },
    "config": {
        "sort-packages": true
    }
}
